# GrammarQL

Ask questions about your data in plain English. GrammarQL uses GPT-5's context-free grammar constrained decoding to generate **syntactically valid SQL by construction** — no prompt engineering tricks, no post-hoc validation hacks. The grammar guarantees every query is valid before it's even finished generating.

**Live demo: https://grammer-ql.vercel.app**
